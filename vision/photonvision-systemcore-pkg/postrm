#!/bin/sh
# Runs on the SystemCore after the package files are removed.
PACKAGE_NAME="photonvision"

systemctl daemon-reload
systemctl reset-failed "$PACKAGE_NAME.service" 2>/dev/null || true

echo "Service cleanup complete."
exit 0
